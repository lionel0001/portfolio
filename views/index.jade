extends layout



block content
   
   #bluestrip

   #container

      include _topnav

      #metaNav
         left
            h3 
               span.blue Selected projects

         right
            h3= site.posts.length

      #projects

         //- p= JSON.stringify(site)
         //- p= JSON.stringify(site.posts[0].year)

         for post in site.posts
            .project
               a(href=post._url)
                  .box(class='bgImg-'+post.id)
                     .veil
                     h1= post.title
                     h2= post.subtitle
                        if post.moreinfo
                           span.moreInfo= ' — '+post.moreinfo 
